<!-- 
<head>
  <link rel="stylesheet" href="/styles.css" />
</head>

<h1>Lista de productos</h1>
<div class="producto-card">
<ul>
  {{#each productos}}
    <li class="producto">
      <img src="${p.thumbnails[0]}" alt=" {{this.title}}" class="producto-img" />
      <h3>{{this.title}}</h3>
      <p>{{this.description}}</p>
      <p><strong>Categoría:</strong> {{this.category}} - <strong>Código:</strong> {{this.code}}</p>
      <p><strong>Stock:</strong> {{this.stock}} - <strong>Precio:</strong> ${{this.price}}</p>
    </li>
  {{/each}}

</ul>
</div>
-->

<h1>Productos</h1>

<div class="products-list">
  {{#each products}}
    <div class="product-card">
      <h3>{{this.title}}</h3>
      <p>Precio: ${{this.price}}</p>
      <p>Categoría: {{this.category}}</p>
      <a href="/products/{{this._id}}">Ver detalles</a>

      <form action="/api/carts/{{cartId}}/products/{{this._id}}" method="POST" style="margin-top:8px;">
        <button type="submit">Agregar al carrito</button>
      </form>
    </div>
  {{/each}}
</div>

<div class="pagination">
  {{#if prevLink}}
    <a href="{{prevLink}}">⬅ Página anterior</a>
  {{/if}}

  <span>Página {{page}} de {{totalPages}}</span>

  {{#if nextLink}}
    <a href="{{nextLink}}">Página siguiente ➡</a>
  {{/if}}
</div>
